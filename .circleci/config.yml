version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:13-browsers-legacy
    steps:
      - checkout
      - run:
          name: Build and Test
          command: |
            npm install
            npm run build
            npm run coverage
            npm run dev
            npm run selenium
